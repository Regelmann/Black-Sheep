# v-BS-PLATFORM-V13.4

**560/560 tests · smoke ✅ · las seis capas verdes**

## El smoke, cerrado

```
Only URLs with a scheme in: file, data, and node are supported by the
default ESM loader. On Windows, absolute paths must be valid file:// URLs.
Received protocol 'c:'
```

`await import(path.join(...))` devuelve `C:\...` en Windows, y Node exige
`file://` para rutas absolutas. En Linux funciona igual, así que era otro bug
invisible desde acá.

Corregido con `pathToFileURL()`. **Las 9 páginas montan.**

## Quinta variante de la misma familia

| | Versión |
|---|---|
| `tsc.cmd` con `execFileSync` | V13.0.1 |
| Barras invertidas en el guard | V13.0.2 |
| 3 tests con `.pathname` | V13.1 |
| `smoke-render.mjs` con `.pathname` | V13.3.1 |
| `import()` sin `file://` | **acá** |

**Guard R20** nuevo: detecta cualquier `import()` con ruta absoluta que no
pase por `pathToFileURL`. Con R19 y R20, las cinco variantes quedan cubiertas
antes de llegar a tu máquina.

## Lo que ya está en GitHub

```
859e11c..2fe6aea  main -> main
```

El rebase funcionó y el push salió. Tu repo está al día.

## Dos cosas que vale mirar cuando haya tiempo

**560 tests, no 549.** `main` tenía más de los que yo conocía: la integración
con la otra herramienta viene funcionando bien.

**19 avisos de RLS abierta:**

```
[R11 política abierta]  sql/08_PROSPECTOS_RLS.sql:18 — using(true)
[R11 política abierta]  sql/13_ADMIN_PANEL.sql:32 — using(true)
... 17 más en 13, 14, 15, 17, 19, 20
```

No bloquean hoy porque hay un solo tenant. **Con un segundo cliente, esas
políticas significan que una empresa puede ver los datos de otra.** El
`28_RLS_ESTRICTO.sql` cierra las principales, pero estos archivos viejos
siguen con `using(true)`.

Es lo que hay que resolver antes de vender la segunda instancia — no antes.
