/**
 * CaseStudy — desactivado a propósito.
 *
 * Antes mostraba "AndesAlimentos", un caso de éxito con nombre de empresa,
 * persona (Camila Rojas), cargo y cifras específicas ("+18% ticket",
 * "Verificado") — pero no correspondía a un cliente real. Presentado como
 * testimonio/caso medido, eso es publicidad engañosa.
 *
 * Se deja el componente en el árbol (por si algo más lo importa) pero sin
 * renderizar nada, hasta que haya un caso de éxito real y con consentimiento
 * del cliente para publicarlo. Cuando lo haya, reemplazar este archivo con
 * los datos reales — la estructura visual (Reveal, barras de progreso,
 * blockquote) puede reutilizarse tal cual, ver el historial de git de este
 * archivo para el markup original.
 */
export default function CaseStudy() {
  return null;
}
