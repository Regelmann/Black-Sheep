/**
 * Testimonials — desactivado a propósito.
 *
 * Las 3 tarjetas (Camila Rojas / AndesAlimentos, Jorge Muñoz / Ferretería
 * El Cóndor, Paulina Vera / AgroSur Insumos) tenían nombre, cargo, empresa
 * y cifras específicas ("+18%", "11 clientes recuperados") pero ninguna
 * corresponde a un cliente real — mismo problema que CaseStudy.tsx.
 *
 * Se deja el componente sin renderizar nada hasta tener testimonios reales
 * (con consentimiento del cliente para publicarlos). Ver el historial de
 * git de este archivo para el markup original si se quiere reutilizar la
 * estructura visual con datos reales más adelante.
 */
export default function Testimonials() {
  return null;
}
