================================================================================
BLACK SHEEP · V74 FIX FIELD
Stamp: v-BS-V74-FIX-FIELD
Build: OK
================================================================================

FIXES
- Stock compradores: match por nombre + SKU normalizado (no solo código)
- Pedido: precio desde lista (stock) si no hay histórico
- Visita: un solo CTA Tomar pedido (sticky); menos duplicado
- Gerencia mix: si sku_detalle trae ≤1 SKU → ventas_lineas
- UX denser + sheets sobre la nav
- /dashboard → redirect a app.black-sheep.cl

DEPLOY
  cd ~/Black-Sheep/Black-Sheep
  # copiar BLACKSHEEP\ encima
  git add -A && git commit -m "V74 fix field" && git push

NOTA DATOS
- Si stock.precio_* está vacío en Supabase, el ciclo debe publicar lista de precios
- Sur Capital 1 SKU: tras deploy, abrir mix fuerza ventas_lineas
================================================================================
